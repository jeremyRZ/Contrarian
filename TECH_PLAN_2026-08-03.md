# 分步实施技术方案：回测 → LLM 决策层 → 策略参数化 UI

> 项目：Contrarian 港股错杀猎手（FastAPI + 富途 OpenD）
> 日期：2026-08-03
> 目标：在现有「6 大策略 + 八档反向信号」规则引擎之上，依序补齐
> **历史回测验证**、**LLM 决策层**、**可视化策略参数 UI**，使系统从「规则脚本」
> 升级为「可验证 + 可解释 + 可配置」的研究系统。

---

## 0. 模块依赖与衔接关系（总览）

```
        ┌─────────────────────────────────────────────────────────┐
        │            strategy_config.py  (参数唯一真源)             │
        │   6 策略阈值/权重 + 反向 8 块权重 + 推送门槛 + LLM 开关     │
        └───────────────────────────┬─────────────────────────────┘
                                     │ 注入 cfg
                    ┌────────────────┴────────────────┐
                    ▼                                 ▼
        ┌──────────────────────┐          ┌──────────────────────────┐
        │  evaluate_signals()  │◄─────────┤   screener.screen() 实盘  │
        │  纯函数：信号计算     │          │   (盘中扫描/分析页)        │
        └──────────┬───────────┘          └──────────────────────────┘
                   │ 同一函数
                   ▼
        ┌──────────────────────┐  产出 buy 信号序列
        │   backtest.py (阶段1) │──────────► BacktestReport
        │   历史重放 + 统计      │            (每策略胜率/样本/显著性)
        └──────────────────────┘                    │
                                                    │ 作为输入
                                                    ▼
        ┌──────────────────────┐          ┌──────────────────────────┐
        │ decision.py (阶段2)   │◄─────────┤  实时信号 + 回测可信度      │
        │ LLM 决策层（被可信度门控）│         └──────────────────────────┘
        └──────────────────────┘
                   │
                   │ 参数变化 → 重新回测 → 重新门控 LLM（阶段3 闭环）
                   ▼
        ┌──────────────────────┐
        │ strategies.html (阶段3)│  参数表单 → POST /strategies/config
        │  策略参数化 UI         │         → 保存并可选「重跑回测」
        └──────────────────────┘
```

**衔接契约（前一阶段输出 = 后一阶段输入）：**
- 阶段1 输出 `BacktestReport`（每策略 `{n, win_rate, avg_ret, max_dd, profit_factor, sharpe, confidence}`）→ 阶段2 用它门控 LLM、阶段3 用它做「保存并回测」的回显。
- 阶段2 输入 = 实盘信号（`screen()`/`analyze` 产出） + 阶段1 的 `BacktestReport`；输出 `Decision`（可被前端直接渲染）。
- 阶段3 输入 = `strategy_config`（驱动全部参数）；其「保存并回测」动作回调阶段1，形成闭环。

---

## 1. 前置重构（阶段1 的起点，必须先行）

当前 `screener.screen()` 把信号计算**内联**在循环里（深度超跌/放量突破/低估值高股息/恒科急跌/恐慌急跌/龙头 6 段 if）。回测要复用**同一套信号逻辑**，否则回测验证的是另一套规则，毫无意义。

**动作：** 抽取纯函数
```python
# app/modules/screener.py
def evaluate_signals(features: dict, cfg: "StrategyConfig") -> dict:
    """给定单日特征快照，返回 {score, signals[], total_score?}。
    features: {code, name, price, change_rate, prev_close_price,
               turnover_rate, amplitude, pe, hi52, lo52, is_leader}
    cfg: 来自 strategy_config 的策略参数（阈值/权重/开关）
    纯函数、无 IO、可重放 —— 实盘与回测共用。"""
```
- `screen()` 内部循环改为调用 `evaluate_signals(row_features, cfg)`。
- `hstech_drop`（市场级）作为 `cfg`/参数传入，不内联。
- 6 策略的硬编码阈值（drop>25、chg>2 & turn>1.5、pe<10 & pos_pct<30、hstech<=-2、chg<-5 & turn>2）迁移为 `cfg` 字段，带默认值。

**验收：** 重构后 `screen()` 行为与重构前字节级一致（跑一次 `/strategies/scan` 对比 top 列表）。

---

## 2. 阶段1：回测模块（信号有效性历史验证）

**新增文件：** `app/modules/backtest.py`
**新增依赖：** 复用 `futu_client.history_kline()`（已存在，返回日 K DataFrame）。

### 2.1 历史特征重建
`get_history_kline(code, K_DAY, max_count=N)` 提供日 K（open/high/low/close/volume）。
从日 K 重建 `evaluate_signals` 所需特征：
| 特征 | 来源 |
|---|---|
| price / change_rate / amplitude | 日 K 直接计算 |
| hi52 / lo52 | 对滚动 250 交易日窗口取 high 最大值 / low 最小值 |
| turnover_rate | 日 K 无此字段 → 用 `volume / 该窗口均量` 近似（放量判定） |
| pe | 日 K 无 → 取当前快照 PE 作代理（PE 慢变，文档标注近似）|
| is_leader | 静态 `LEADERS` 集合 |
| hstech_drop | 对指数 `HK.800700` 单独拉日 K 计算 |

### 2.2 核心接口
```python
def build_history_features(client, code: str, window_days: int) -> list[dict]:
    """返回按交易日排序的特征列表（每元素 = evaluate_signals 的 features）。"""

def simulate_forward(features_seq: list[dict], buy_days: list[int],
                     forward_days: int) -> list[float]:
    """对命中 buy 的交易日，计算持有 forward_days 后的收益率序列。"""

def run_backtest(codes: list[str], cfg, client,
                 from_date=None, to_date=None,
                 forward_days: int = 20) -> dict:
    """返回 BacktestReport：
    {
      per_strategy: { 策略名: {n, win_rate, avg_ret, profit_factor,
                                max_drawdown, sharpe, confidence} },
      per_code: { code: {entries, best, worst} },
      overall: {...},
      generated_at, window, forward_days
    }
    confidence 分级（对齐 stock-strategies-only 做法）：
      n < 8  → '样本不足(无统计意义)'
      8<=n<20 → '弱可信'
      n>=20   → '相对可信'
    }"""
```
- 统计指标：胜率=收益率>0 占比；盈亏比=平均盈利/平均亏损绝对值；最大回撤=持有期内最大峰谷回撤；夏普=收益均值/标准差×√年化因子。
- **信号归因**：buy 常由多信号叠加，按策略分别统计「该信号单独出现日的 forward 收益」，输出每策略独立胜率。

### 2.3 存储（复用 `app/cache.py`）
- K 线缓存：key=`kline:{code}:{window}`，避免重复下载（富途限频）。
- 回测结果缓存：key=`backtest:{codes_hash}:{forward_days}:{window}`，TTL 24h。

### 2.4 API
- `GET /backtest/report?codes=&forward_days=20&window=250` → 返回 `BacktestReport`（读缓存，秒级）。
- `POST /backtest/run` body `{codes, forward_days, window}` → 异步重算并写缓存，返回 task_id。

### 2.5 前端 `backtest.html`（新增页，导航加「回测验证」）
- 每策略一行：胜率（带色块）、样本数、平均收益、盈亏比、最大回撤、夏普、可信度徽章。
-  Equity curve 折线图（组合持有收益曲线）。
- 顶部表单：候选池选择（leaders / 观察池 / 自定义 codes）、持有天数、回测区间。

**阶段1 验收：** 对任意 code 跑出 `BacktestReport`；`confidence` 徽章正确；结果与手算抽样一致。

---

## 3. 阶段2：LLM 决策层（回测通过后接入）

**前提：** 阶段1 的 `BacktestReport` 已产出。
**复用：** `app/modules/news.py` 已有 OpenAI 兼容 LLM 传输（`llm_review`，读 `config.news.llm.{base_url,api_key,model}`），抽成通用客户端避免重复。

### 3.1 新增文件
- `app/modules/llm_client.py`：通用封装 `call_llm(system, user, cfg) -> str | None`（超时、失败降级返回 None）。
- `app/modules/decision.py`：决策编排。

### 3.2 门控规则（显式衔接阶段1）
> 仅在「该标的命中的策略「回测可信度 = 相对可信」且 win_rate ≥ cfg.llm.min_win_rate」时才调用 LLM。
> 未通过门控 → 不调 LLM（省成本），前端不显示决策卡。这是「回测验证通过后接入」的硬约束。

### 3.3 核心接口
```python
# app/modules/decision.py
def build_decision_prompt(signal: dict, backtest_cred: dict) -> str:
    """把实时信号(6策略命中+八档反向+现金状态)与回测可信度压成结构化 prompt。"""

def decide(signal: dict, backtest_cred: dict, cfg) -> Optional[dict]:
    """返回 Decision 或 None（未配置/未过门控/调用失败）。
    Decision = {
      verdict:   '强烈关注'|'可建仓'|'观察'|'回避',
      thesis:    str,            # 自然语言猎手观点
      entry:     float|None,     # 建议建仓位
      stop:      float|None,     # 止损位
      target:    float|None,     # 目标位
      confidence: float,         # 0~1
      raw:       str             # 原始 LLM 输出(调试)
    }"""
```

### 3.4 配置（`config.yaml` 新增 `llm:` 块，与 `news.llm` 合并）
```yaml
llm:
  enabled: false
  base_url: https://api.deepseek.com/v1   # 国内可达，默认 DeepSeek
  api_key: ''
  model: deepseek-chat
  min_win_rate: 0.55          # 门控：回测胜率阈值
  timeout: 15
```

### 3.5 集成点
- `app/modules/analyze.py`（分析页主流程）：计算完 `signal` 后，若 `cfg.llm.enabled`，拉取该 code 的 `BacktestReport`（缓存/实时）做门控，过则 `decide()`，把 `decision` 挂到返回体。
- `screen()` 批量场景：默认不调 LLM（成本高），仅单票分析页/手动「生成观点」时调。

### 3.6 API
- `GET /decision?code=HK.00700` → 实时计算信号 + 回测可信度 + 调 LLM，返回 `Decision`。

### 3.7 前端
- `analyze.html` 新增「🤖 猎手观点」卡片：verdict 徽章 + thesis + 建仓/止损/目标三栏。
- `strategies.html` 行可展开看 `Decision`（可选，阶段3 同页）。

**阶段2 验收：** 配置 `llm` 后，对回测可信标的返回结构化 `Decision`；未过门控标的返回 null；无 `api_key` 时整卡隐藏、不影响其余功能。

---

## 4. 阶段3：策略参数化 UI（最后，闭环）

**前提：** 阶段1（回测）、阶段2（LLM 门控）均已落地，`evaluate_signals` 已吃 `cfg`。
**目标：** 用户不碰代码即可调参，且改参 → 重跑回测 → 刷新可信度 → 重新门控 LLM。

### 4.1 新增文件 `app/modules/strategy_config.py`
```python
# 参数唯一真源（从 config.yaml 拆出独立文件 strategies.yaml，避免频繁改主配置）
DEFAULT_STRATEGY_CONFIG = {
  "strategies": {
    "deep_drop":     {"enabled": True,  "weight": 3.0, "drop_pct": 25.0},
    "vol_breakout":  {"enabled": True,  "weight": 3.0, "chg_pct": 2.0, "turn": 1.5},
    "low_pe_high_div":{"enabled": True, "weight": 3.0, "pe": 10.0, "pos_pct": 30.0},
    "hstech_link":   {"enabled": True,  "weight": 2.0, "hstech_drop": -2.0},
    "panic_drop":    {"enabled": True,  "weight": 2.0, "chg_pct": -5.0, "turn": 2.0},
    "leader_pool":   {"enabled": True,  "weight": 1.0},
  },
  "reverse_weights": {        # 八档反向 8 块权重（复用 reverse_signals 现有权重）
    "southbound": 2.0, "buyback": 2.0, "news": 1.5, "capital_flow": 1.5,
    "valuation": 2.0, "institution": 1.5, "dividend": 1.5, "earnings": 0.5
  },
  "push": {"light": 6.0, "mid": 7.0},
  "llm":  {"use_llm": True, "min_win_rate": 0.55},
  "backtest_accept": {"min_win_rate": 0.55, "min_sample": 20}
}
def load_config() -> dict          # 读 strategies.yaml，merge 默认值
def save_config(cfg: dict) -> dict  # 校验后落盘 strategies.yaml
```
- `evaluate_signals` / `screen` / `run_backtest` 全部读此 `cfg`。

### 4.2 API
- `GET /strategies/config` → 返回当前 `cfg`（前端表单初始化）。
- `POST /strategies/config` body=`cfg` → 校验 → 落盘 `strategies.yaml` → 返回生效配置；若带 `rerun_backtest=true` 则触发阶段1 重算并回显 `BacktestReport`。

### 4.3 前端（扩展 `strategies.html`）
- 新增「⚙ 参数配置」折叠区：每策略一行 = 开关 + 阈值输入/滑块 + 权重；反向 8 块权重；推送门槛；LLM 开关。
- 按钮：「保存」（仅落盘）/「保存并回测」（落盘 + 重跑回测，刷新可信度徽章）。
- 保存后实时刷新顶部「回测验证」表格（阶段1 数据），形成闭环。

**阶段3 验收：** 改 `deep_drop.drop_pct` 从 25→30 保存 → `/strategies/scan` 命中数变化；点「保存并回测」→ 可信度徽章刷新；全程无需改代码。

---

## 5. 跨阶段一致性清单

| 项 | 现状 | 阶段后 |
|---|---|---|
| 信号逻辑 | `screen()` 内联 | 抽 `evaluate_signals(features, cfg)` 纯函数 |
| 参数来源 | 硬编码 | `strategy_config.py` + `strategies.yaml` |
| LLM 传输 | 仅 `news.py` 内用 | 抽 `llm_client.py` 通用封装 |
| 回测数据 | 无 | `history_kline` + `cache.py` |
| 配置 | `config.yaml` | 主配置 + `strategies.yaml` + `llm` 块 |
| 导航 | 分析/策略/回测? | 新增「回测验证」页 |

## 6. 里程碑与交付顺序

1. **M1（阶段1 前置）**：抽取 `evaluate_signals` + `strategy_config` 默认值；`screen()` 行为不变。✅ 门控点。
2. **M2（阶段1）**：`backtest.py` + K线缓存 + `/backtest/*` + `backtest.html`。交付回测验证能力。
3. **M3（阶段2）**：`llm_client.py` + `decision.py` + 门控 + `/decision` + 分析页决策卡。交付差异化 LLM 观点。
4. **M4（阶段3）**：`strategy_config` 落盘 + `/strategies/config` + `strategies.html` 参数面板 + 「保存并回测」闭环。交付可配置系统。

每阶段独立可验证、可回滚；前一阶段验收通过再进下一阶段。

## 7. 实现状态（2026-08-03 全量落地）

M1–M4 已全部交付并验证通过（服务运行于 http://127.0.0.1:8000）：

- **M1** ✅ `evaluate_signals(f, cfg)` 纯函数解耦；`screen()` 行为零差异（4 单测守护）；`strategy_config.py` 唯一真源（6 策略 + 反向 8 块 + 推送 + LLM 门控 + 回测验收）。
- **M2** ✅ `backtest.py`（兼容壳，re-export `bt_backtest`）：原逐日「前向收益」重放已**全量改写为 backtrader 真实成交级回测**（见 §7.5）。分策略胜率/盈利因子/赔率/最大回撤/夏普 + 可信度徽章（n<8 无意义 / 8–20 弱可信 / ≥20 相对可信）。
  - 数据忠实度两根因已修复：①`history_kline` 加 `start/end`（取近期数据，末日 2026-07-30）；②日 K `turnover_rate` 小数 ×100 对齐实盘快照百分比量程。
  - API：`GET /backtest/report`、`POST /backtest/run`（均含 `no_fee` 参数）；新增 `GET /backtest/debug`（逐根 bar 直跑 `evaluate_signals` 诊断信号频次）、`GET /backtest/sweep`（网格寻优）；前端 `backtest.html` + 导航「回测验证」+ `.inp/.bar` 样式（新增盈利因子/赔率/诊断行）。
- **M3** ✅ `llm_client.py`（OpenAI 兼容，复用 news.py urllib 模式，无 key 自动降级）+ `decision.py`（回测门控 → 差异化判断）。
  - 门控：`strategies.yaml.llm.min_win_rate`（默认 45%）且可信度 ≠ 样本不足才调 LLM；未配置 key 则 `gated=True` 并返回明确原因。
  - 历史实测曾引用 MOC 口径整体胜率 58.1%，该数字因前视偏差已失效；门控必须读取修正口径的最新缓存报告。无 key 时返回 `gated=True / 未启用或未配置 LLM`。Mock LLM 验证 prompt→调用→JSON 解析全链路 ok（verdict/per_signal/rationale/risk/position）。
  - API：`GET /decision`；前端 `analyze.html` 新增「🤖 猎手观点」卡片（异步加载，gated 时显示回测参考 + 配置提示）。
- **M4** ✅ `strategies.yaml` 落盘（独立于 config.yaml）+ `GET/POST /strategies/config`（深合并、校验、`_validate`）+ `POST /strategies/config/reset`。
  - 前端 `strategies.html` 参数面板：6 策略开关/权重/阈值、反向 8 块权重、推送门槛、LLM 开关/门控门槛；「保存」「保存并回测」「恢复默认」三按钮，保存即清空回测缓存生效。
  - 配置两处归属已厘清：`config.yaml.llm`（api_key/base_url/model 等密钥/端点，手写）、`strategies.yaml.llm`（enabled + min_win_rate，UI 可调）。

### 7.5 backtrader 回测引擎重写（第二轮 · 2026-08-03）

用户要求：①回测改用 **backtrader** 真实成交级回测；②旧版整体胜率仅 45–48% 太低，需联网检索高胜率策略并改进。两项均已完成并验证。

**核心引擎 `app/modules/bt_backtest.py`**（替代原逐日前向重放）：
- backtrader 1.9.78.123 接管 **信号执行 + 交易级统计 + 成本建模**；模块边界保持：`screener.evaluate_signals(f, cfg)` 仍是唯一信号源，backtrader 只执行/统计/成本。
- `FeatureData`（`bt.feeds.PandasData`）按位置映射 0–13 列：open/high/low/close/volume/turnover/pe/hi52/lo52/pos_pct/sma50/sma200/rsi2/hstech_crash。
- `HKCommission(bt.CommissionInfo)`：佣金 `rate=0.0005` 双边、卖出加印花税 `stamp=0.001`（港股单边），实现 `_getcommission(size, price, pseudoexec)` 钩子。
- **双模式回测**（解决「综合分永远不达标 → 0 成交」）：
  - `combo` 模式：综合分 ≥ 推送门槛（6.0）才入场 → 对应**实盘推送口径**，用于 `overall` / `per_code`；
  - `single` 模式：目标信号出现即入场 → 用于**逐策略胜率统计**（样本量大，RSI2 n=193 / 恒科 n=446 等）。
- **历史 MOC 成交口径**（`set_coc=True`）：该口径会使用当日收盘后才完整形成的信号在同一收盘成交，存在前视偏差；2026-08-07 起默认改为次日开盘成交，仅保留为显式乐观情景开关。
- `notify_order` 录 `entry_price/entry_size`；`notify_trade` 用 `entry_size*entry_price` 算名义本金（平仓后 `trade.size` 归零会导致收益恒 0）。

**高胜率策略调研结论**（联网检索）：Connors **RSI(2)**——价 > 200 日线 + RSI(2) < 阈值买入、RSI(2) > 阈值或跌破 5 日线卖出，实证单标的胜率 64–75%；共性 = **趋势过滤器（只做多头的回调）+ 短期超卖** 双条件。据此新增 `rsi2_connor` 策略（价 > sma200 + RSI2 < oversold 买，趋势内抄底），并将 5 个下跌类信号统一加趋势过滤器（`trend_filter.apply`）。

**网格寻优 `/backtest/sweep`**（45 只龙头池 / 250 日 / 12 组，约 13m38s）结论：
- `rsi2_oversold` < 5 优于 < 10（胜率 61.1% vs 59.6%，期望 +0.28% vs +0.16%）；
- `forward_days=3` 太短（胜率跌至 48%），`forward_days=10` 最优；
- `stop_pct=0.08` 深止损让亏损单拖累赔率，`stop_pct=0.04` 更优；
- 历史 MOC 口径最优组 → `forward_days=10 / stop_pct=0.04 / rsi2_oversold=5` → 整体胜率 **58.1%**、期望 **+0.19%**、盈利因子 **1.10**；该结果存在前视偏差，不再作为当前决策依据。

**最终验证（重启服务 + 全池回测，2026-08-03 22:09）**：
- `codes_used=45`、`bars_tested=11220`、`with_cost=true`、`order_stats.Completed=2087`、`skipped={}`、`max_score=10.0`（推送门控生效）。
- 逐策略胜率：`RSI2 逆向低吸 61.1% (n=193)`、`深度超跌反弹 59.0% (n=117)`、`恒科联动 54.3% (n=446)`、`放量突破 51.5% (n=229)`、`异常急跌逆向 40.4% (n=52)`、`低估值高股息 80% (n=5, 样本不足)`。
- 历史 MOC 口径整体（combo）胜率 **58.1%**、盈利因子 **1.10**、赔率 0.79；原“夏普/最大回撤”实际是单笔样本统计，且整体结果含前视偏差，已失效，须按次日开盘成交重新验证。
- 门控不得继续引用历史 58.1%；应以修正口径重跑后的缓存报告为准。

**修正口径验证（FutuOpenD + 全池回测，2026-08-07 10:27）**：
- 口径：`exec_on_close=false`（次日开盘成交）、`forward_days=10`、`window_days=250`、`with_cost=true`。
- 数据完整性：`codes_used=45/45`、`bars_tested=11224`、`skipped={}`、`order_stats.Completed=2049`、`Margin=3`。
- 组合（combo）：`n=42`、胜率 **59.5%**、平均每笔 **+1.34%**、盈利因子 **1.74**、赔率 **1.18**、最差单笔持有期回撤 **9.64%**。样本量仍有限，不等同于组合净值最大回撤或年化夏普。
- 单策略：RSI2 逆向低吸 **63.9% (n=191)**、深度超跌反弹 **61.2% (n=116)**、放量突破 **48.2% (n=224)**、恒科急跌联动 **49.9% (n=435)**、异常放量急跌 **42.3% (n=52)**；低估值高股息 **80.0% (n=5)** 仍属样本不足。
- 当前证据显示 RSI2 与深度超跌具正期望；恒科联动和异常放量急跌为负期望，后续应优先复核或降权，而不是仅依靠总体胜率放行。

**修复的 5 个根因（backtrader 跑通关键）**：
1. `build_feature_frame` 索引错位 → K 线全 NaN（`out` 用 DatetimeIndex、`df` 用 RangeIndex，按标签对齐全空）；改为先 `df.index=pd.to_datetime(dates)` 再建 `out`。
2. combo 模式单标的 0 成交 → 综合分最高仅 5.0 < 推送门槛 6.0；实现 dual-mode（single 模式按单信号触发）。
3. 缺 `notify_order` → `self.order` 永不清空，首买后永久 `return` 不交易；补 `notify_order` 清 `self.order` 并录 `entry_*`。
4. `HKCommission.getcommission` → backtrader 钩子是 `_getcommission(size, price, pseudoexec)`，重写 `getcommission` 致每次下单 TypeError；改名 `_getcommission`（也解释了为何此前 combo 模式未暴露此错）。
5. `notify_trade` 名义本金 = 0 → 平仓后 `trade.size` 归零，`notional=0` → 收益恒 0；在 `notify_order` 的 buy Completed 时录 `entry_size=abs(order.executed.size)` 修正。

**关键经验（后续复用）**：
- 富途 `request_history_kline` 不带日期返回陈旧数据；必须传 `start/end`（≈窗口 1.5×日历日缓冲）才返回近期 K 线。
- 日 K `turnover_rate` 为小数、实盘快照为百分比，回测需 ×100 才与 `evaluate_signals` 量程一致（否则放量/恐慌类信号 n=0）。
- 回测「某单只大市值」可能天然不触发放量/价值类信号（Tencent 换手仅 ~0.25%、PE~17），验证这类策略须跨全龙头池。
- `decision` 配置来源：回测参数来自 `strategy_config`（`backtest.cached_report(cfg=None)` 内部加载），LLM 密钥来自 `config.yaml`，门控阈值来自 `strategies.yaml`——三者不要混用同一 `cfg`。
