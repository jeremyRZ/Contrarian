# Contrarian 一键启动

桌面快捷方式名称：`Contrarian 港股决策系统`

双击后会自动：

1. 检查并尝试启动 Futu OpenD；
2. 使用 `G:\Coding\envs\ContestTrade\python.exe` 启动本项目；
3. 等待后端健康检查通过；
4. 打开 `http://127.0.0.1:8000/strategy-center.html`。

重复双击会复用现有服务，不会重复启动多个后端。服务日志保存在 `.runtime/launcher/`。

当前生产决策链以代码中的三套已准入日线策略为准：小米趋势、港股 200 日风险调整动量、港股长期新高突破。SuperTrend 目前只做影子验证；日内策略因样本外门槛未通过而保持禁用。系统为只读建议模式，不会自动提交真实订单。
