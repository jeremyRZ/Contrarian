# 港股错杀猎手 · 手动交易操作手册

> 基于 backtrader 验证参数：整体胜率 **58.1%** / 盈利因子 **1.10** / 单笔止损 **4%** / 持有期 **10 个交易日**
> 适用版本：决策支持系统（**不自动下单**，富途 OpenD 仅用于行情与扫描）
> 更新：2026-08-03

---

## 0. 先认清边界（必读）

- 本系统是 **「决策支持 + 信号推送」**，不是自动交易机器人。后端没有任何下单接口。
- **下单在你手机/电脑的富途牛牛 App 里手动完成**；系统负责「找机会 + 给买卖点 + 风控提醒」。
- 58.1% 胜率 + 0.79 赔率是**历史回测**结果，实盘有滑点、跳空、情绪干扰，先小资金练手。
- 盈利因子 1.10 = 薄利边缘，纪律（尤其止损）比选股更重要。

---

## 1. 每日盘中流程（约 15–30 分钟）

### 步骤 1 · 扫描候选（2 分钟）
- 打开 `strategies.html` → 点「刷新」；或命令行：
  ```bash
  curl "http://127.0.0.1:8000/strategies/scan?top_n=50"
  ```
- 看 topN 打分，**挑综合分 ≥ 6（轻仓门槛）** 的标的进入下一步。

### 步骤 2 · 过滤高质量信号（3 分钟）
- 打开 `backtest.html` → 看各策略「胜率 / 盈利因子 / 可信度徽章」。
- **只做「相对可信」且胜率 > 55% 的策略**：
  | 策略 | 胜率 | 样本 | 备注 |
  |---|---|---|---|
  | RSI2 逆向低吸 | 61.1% | 193 | 趋势内抄底，首选 |
  | 深度超跌反弹 | 59.0% | 117 | 超跌 + 趋势过滤 |
  | 恒科急跌联动 | 54.3% | 446 | 样本大，稳健 |
  | 放量突破 | 51.5% | 229 | 临界，谨慎 |
- **避开**：异常急跌逆向（40.4%，样本不足）、低估值高股息（n=5 无统计意义）。

### 步骤 3 · 单票决策（每票约 2 分钟）
- 打开 `analyze.html` 输入代码 → 看「🤖 猎手观点」卡片。
- 门控通过（回测可信 + 胜率 ≥ 45%）才出现：**verdict + 建仓价 + 止损位 + 目标位**。
- 未配 LLM key 时卡片显示回测参考，由你自行判断。
- 命令行：
  ```bash
  curl "http://127.0.0.1:8000/decision?code=HK.00700"
  ```

### 步骤 4 · 推送兜底（可选）
- 达标信号推企业微信，通勤也能看：
  ```bash
  curl -X POST "http://127.0.0.1:8000/strategies/push?force=true"
  ```

---

### 推送长什么样（每条都带原因）

企业微信收到的买入信号现在包含 **分数来源 + 原因 + 回测背书 + 止损**，例如：

```
🔵 6大策略买入信号
> ASMPT(HK.00522)
> 现价: 150.3  涨跌: -0.66%
> 基础分: 6.0  反向加分: +0  总分: 6.0
> 分数来源: 深度超跌反弹+3.0 + 龙头观察池+1.0 + RSI2 逆向低吸+2.0
> 原因: 上升趋势中：距52周高点回撤38%（超跌区）；RSI2=1 极度超卖（<5） → 趋势内回调/低吸，非接飞刀
> 回测背书: 深度超跌反弹 回测胜率约59.0%/盈利因子0.95；RSI2 逆向低吸 回测胜率约61.1%/盈利因子1.19
> 仓位: 轻仓(现金7%)
💡 首批建仓≤1/3，止损 -4%（与回测一致）
```

> 「回测背书」默认用验证过的静态数值；跑过一次 `backtest/report` 后自动切换为实时缓存的「历史胜率/盈利因子」（无"参考"字样）。

## 2. 在富途 App 手动下单（关键动作）

1. **买入**：用**限价单**，价格填系统给的「建仓价」——**别市价追高**（回测按收盘价成交，追高会吃掉均值回归利润）。
2. **仓位**：单票 ≤ 总资金 **30%**；按公式倒推：
   `单票仓位% = 单笔风险% ÷ 止损%`，例：风险 1.5% ÷ 4% ≈ 37.5% 上限 → 取 30%。
3. **止损**：挂「止损限价单」，或至少设价格报警，触发 **-4%** 立即走。
4. **绝不加仓摊平**：均值回归策略摊平 = 接飞刀。

---

## 3. 盯盘与离场

- `holdings` 看持仓；`price-alerts` 设报警（建仓价 -4% 止损线、目标位各一条）。
  ```bash
  curl "http://127.0.0.1:8000/holdings"
  curl "http://127.0.0.1:8000/price-alerts"
  ```
- **离场三选一（先到先走）**：
  1. 触发止损（-4%）→ 走；
  2. 到达目标位 → 走；
  3. 持有满 **10 个交易日**（回测持有期）→ 走。
- 黑天鹅（恒科急跌 / 财报日）：`hstech_crash` 过滤已挡大部分，但**单票限仓**防极端跳空。

---

## 4. 风控检查表（每次下单前过一遍）

- [ ] 信号属于胜率 > 55% 的策略？（RSI2 / 深度超跌 / 恒科联动）
- [ ] 综合分 ≥ 6，或经分析明确认可？
- [ ] 单票仓位 ≤ 30%？
- [ ] 止损位已设（-4%）且「触发就走」写入纪律？
- [ ] 今日总风险敞口 ≤ 总资金 2%？
- [ ] 不在财报前夜 / 重大事件前开新仓？（防跳空打不到止损）

---

## 5. 每周复盘

- 跑 `backtest/report` 看胜率 / 盈利因子是否漂移。
- 记录实盘成交 vs 系统建议，算**真实胜率**。
- 真实胜率 < 50% 或盈利因子 < 1 → 暂停，回 `backtest/sweep` 重新寻优再上。

---

## 6. 端点速查表

| 端点 | 用途 |
|---|---|
| `GET /strategies/scan?top_n=50` | 全龙头池 6 策略扫描打分 |
| `GET /backtest/report` | 各策略胜率 / 盈利因子 / 可信度 |
| `GET /decision?code=HK.xxxx` | 单票 LLM 决策（建仓/止损/目标） |
| `POST /strategies/push?force=true` | 达标信号推企业微信 |
| `GET /holdings` | 持仓监控 |
| `GET /price-alerts` | 价格报警列表 |
| `GET /monitor` | 持仓监控总览 |
| `GET /backtest/sweep` | 参数网格寻优（调参用） |
| `GET /futu-watchlist` · `POST /futu-watchlist` | 同步观察池到富途 |

> 服务地址：`http://127.0.0.1:8000`（需先启动 `uvicorn app.api:app`，且富途 OpenD 已连）。
