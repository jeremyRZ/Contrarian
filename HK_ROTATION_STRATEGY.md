# 港股 200 日风险调整动量 V2

这是面向港股动态流动性池的低频轮动策略。它每 20 个交易日复核一次，只在恒指收盘高于 MA200 时，从满足价格、流动性、个股 MA200 和正动量条件的股票中，按“200 日动量（跳过最近 20 日）/ 60 日年化波动率”排序。

最多选择 4 只股票，每只目标占账户净资产 25%，按实时可用现金和港股整手约束取整。恒指门控关闭时，候选仅供观察；非正式调仓日不生成买卖动作。系统只输出只读建议，不连接真实下单。

当前历史候选结果为累计收益 61.3953%、最大回撤 -14.7148%、盈利因子 2.6111、46 笔交易、覆盖 28 只股票。该结果仍受历史股票池选择偏差影响，所以新增仓位继续接受前向验证，不能视作实盘收益承诺。

唯一运行契约见 `strategies/hk_rotation_v2.yaml`。候选研究入口为 `scripts/research_cross_sectional_rotation.py`，更严格的逐日走查入口为 `scripts/run_walkforward_daily.py`。
